#!/usr/bin/env node
var argv = require('optimist')
    .alias('o', 'open')
    .describe('o', 'Open a browser with the right page')
    .alias('p', 'port')
    .describe('p', 'Select a port')
    .argv;

var showoff = require(__dirname + '/../lib/showoff.js');
showoff.run(argv)