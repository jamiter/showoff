#!/usr/bin/env node
const argv = require('yargs-parser')(process.argv.slice(2))
const showoff = require(__dirname + '/../src/showoff.js');

showoff({
    open: argv.open || argv.o,
    port: argv.port || argv.p,
})
